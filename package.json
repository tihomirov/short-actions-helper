{
  "name": "remote-shortcuts-root",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "lerna": "^6.5.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
